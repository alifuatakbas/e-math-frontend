'use client';

import ResetPassword from './../components/Reset-password';

export default function ResetPasswordPage() {
    return (
        <main>
            <ResetPassword />
        </main>
    );
}